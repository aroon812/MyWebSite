<h1>Edit Project Detail</h1>

<%= form_for @photo, :url =>{:action => "update", :id =>@photo} do |f| %>

<p>Title: <%= f.text_field 'project_id' %></p>
<p>Description<br/>

<%= f.text_area 'description' %></p>
<%= f.submit "Save changes" %>
<% end %>

<%= link_to 'Back', {:action => 'list' } %>